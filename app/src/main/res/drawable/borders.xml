<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/btnColor"> <!-- Màu đào -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" /> <!-- Màu nền -->
        </shape>
    </item>
    <item>
        <shape xmlns:android="http://schemas.android.com/apk/res/android">
            <solid android:color="@android:color/transparent" /> <!-- Màu nền, bạn có thể thay đổi tùy ý -->
            <stroke
                android:width="2dp"
                android:color="@android:color/darker_gray"
                android:top="2dp" /> <!-- Chỉ định viền ở phía trên -->
            <padding android:top="2dp" />
        </shape>
    </item>
</ripple>